{
  "name": "@thetrg/tm-behave",
  "version": "0.1.0",
  "main": "index.js",
  "keywords": [],
  "author": "The TRG, LLC",
  "license": "MIT",
  "description": "A simple test runner for the browser.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "bench": "cd bench && node index.js",
    "bench:watch": "npx nodemon --exec \"pnpm run bench\" --watch src/js/common",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "nodemon": "^3.1.7",
    "vite": "^5.4.8",
    "vite-plugin-istanbul": "^6.0.2"
  }
}
